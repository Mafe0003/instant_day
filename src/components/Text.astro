---
// Henter parametrene fra Astro.props
const { teksttype="brødtekst", stylingclasses=""} = Astro.props; 
 
//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige størrelser.
const sizeClasses = {
  "Light": "font-paragraph text-[var(--color-light)] text-[12px] lg:text-[20px]",
  "Dark": "font-paragraph text-[var(--color-dark)] text-[12px] lg:text-[20px]",
};



---
<div class:list={[sizeClasses[teksttype], stylingclasses]}><slot/></div>


